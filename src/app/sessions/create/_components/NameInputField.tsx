import Input from '@/components/ui/Input';

export default function NameInputField() {
  return (
    <Input
      label="세션 이름"
      id="session-name"
      placeholder="세션 이름을 입력하세요"
    />
  );
}
